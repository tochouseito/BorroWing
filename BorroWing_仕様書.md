# BorroWing 仕様書

作成日: 2026-05-19  
版: Draft 0.1

---

## 1. 前提

本仕様書は、半レール式ハイスピードフライトアクション **BorroWing** のプロトタイプ制作を目的とした仕様である。

本作では、以下を採用する。

- 半レール式フライト
- ロックオンミサイル
- コンバートフィールド
- 小型ミサイルの反転ミサイル化
- 大型ミサイルの推進ブースター化
- 敵残骸のアーマー化
- 3状態成立によるミサイル無限状態

以下は採用しない。

- メック形態
- ルート分岐
- 完全自由飛行
- 専用回避ボタンを中心にしたゲーム性

---

## 2. ゲームクリア条件

プロトタイプでは、1ステージ + 1ボス構成とする。

### クリア条件

```text
最終ボスのコアを破壊する
```

### 失敗条件

```text
自機HPが0になる
```

または、プロトタイプ段階では以下でもよい。

```text
自機の残機が0になる
```

---

## 3. 基本操作

| 操作 | 内容 |
|---|---|
| 移動 | 画面内の上下左右移動 |
| マシンガン | 弾数無限。前方へ連射する通常攻撃 |
| ロックオン | 範囲内に入った敵を即時ロックし、撃破または消滅まで維持する |
| ロックオンミサイル | ロック済み対象すべてへ順番にミサイル発射 |
| コンバートフィールド | 吸収フィールドを展開する |
| ミサイル無限発動 | READY時に特殊状態へ移行する |

---

## 4. 操作方針

本作では、回避アクションを主役にしない。

敵攻撃への対応は以下のように分ける。

| 対象 | 対応 |
|---|---|
| 通常弾 | 通常移動で避ける |
| レーザー | 位置取りで避ける |
| 爆風 | 範囲外へ移動する |
| 小型ミサイル | コンバートフィールドで吸収する |
| 大型ミサイル | コンバートフィールドで吸収する |
| 撃破した敵の残骸 | コンバートフィールドで吸収する |

プレイヤーが判断することは以下である。

```text
この攻撃は避けるべきか
この攻撃は吸収できるか
今フィールドを展開すべきか
吸収後の強化状態をどう使うか
```

---

## 5. 半レール移動仕様

### 5.1 基本

自機はレールに沿って進行する。

プレイヤーは完全自由飛行ではなく、レール上の基準位置から上下左右にオフセット移動する。

```text
自機位置 = レール基準位置 + カメラRight * offsetX + カメラUp * offsetY
```

### 5.2 オフセット移動

`offsetX`, `offsetY` は画面内移動量である。

```text
offsetX: 左右移動
offsetY: 上下移動
```

各値は画面範囲内に制限する。

```text
offsetX = clamp(offsetX, -maxOffsetX, maxOffsetX)
offsetY = clamp(offsetY, -maxOffsetY, maxOffsetY)
```

### 5.3 速度状態と実進行速度

実際のレール進行速度は大きく変えない。

速度状態ごとの目安は以下。

| 状態 | レール進行速度 | 見た目速度 |
|---|---:|---:|
| Cruise | 1.00 | 1.00 |
| Sonic Stream | 1.10 | 1.45 |
| Missile Infinity | 1.15 | 1.70 |

実進行速度よりも、以下を中心に高速感を出す。

- FOV
- 速度線
- ブースト炎
- カメラの軽い引き
- 音圧
- ロックオン範囲
- ミサイル発射テンポ

---

## 6. 自機リソース

自機は以下の主要リソースを持つ。

| リソース | 内容 |
|---|---|
| HP | 自機体力 |
| マシンガン弾数 | 無限。残弾管理しない |
| 反転ミサイル残弾 | 吸収した小型ミサイル由来の特殊弾数 |
| フィールドゲージ | コンバートフィールド展開可能時間 |
| 反転ミサイル状態タイマー | ミサイル無限条件用の火力状態 |
| ソニックストリームタイマー | ミサイル無限条件用の速度状態 |
| アーマー状態 | ミサイル無限条件用の防御状態 |
| ミサイル無限タイマー | 特殊状態の残り時間 |

---

## 7. コンバートフィールド仕様

### 7.1 概要

コンバートフィールドは、自機周囲に展開する円形フィールドである。

吸収可能対象がフィールドに触れると、対象種類に応じて自機リソースへ変換される。

```text
小型ミサイル → 反転ミサイル残弾
大型ミサイル → 推進ブースター / ソニックストリーム
敵残骸       → アーマー
```

### 7.2 操作

推奨仕様は以下。

```text
フィールドボタン長押し: フィールド展開
フィールドボタンを離す: フィールド解除
```

フィールド解除時に吸収弾を自動発射しない。  
小型ミサイルは弾薬化され、発射は通常のミサイルボタンで行う。

### 7.3 展開条件

フィールドは、フィールドゲージがある時のみ展開できる。

```text
フィールドゲージ > 0
  → 展開可能

フィールドゲージ == 0
  → 展開不可
```

### 7.4 フィールド中の処理

```text
1. フィールドゲージを消費する
2. 吸収可能対象との接触判定を行う
3. 対象種類に応じて変換する
4. 吸収済み対象を消去、または状態変更する
```

### 7.5 吸収不可対象

以下はフィールドで吸収できない。

- 通常弾
- レーザー
- 爆風
- 赤い特殊弾
- 地形
- 敵本体

吸収不可対象に当たった場合は、通常通りダメージを受ける。

### 7.6 オーバーロード

プロトタイプ初期では省略してもよい。  
導入する場合は、短時間に吸収しすぎた時にフィールドが不安定化する仕様にする。

例:

```text
一定時間内に吸収コストが上限を超える
  ↓
フィールドオーバーロード
  ↓
一定時間フィールド使用不可
```

---

## 8. 小型ミサイル吸収仕様

### 8.1 役割

小型ミサイルは、火力リソースである。

敵の小型ミサイルを吸収すると、自機の **反転ミサイル残弾** に変換される。

### 8.2 吸収処理

```text
小型ミサイルがフィールドに接触
  ↓
小型ミサイルを消去
  ↓
反転ミサイル残弾 + 1
  ↓
反転ミサイル状態タイマーを更新
  ↓
吸収エフェクト再生
```

### 8.3 反転ミサイル残弾

ロックオンミサイルとは別枠で管理する。

```text
normalMissileAmmo
reverseMissileAmmo
```

UI上でも分けて表示する。

例:

```text
MISSILE 12
REVERSE 04
```

### 8.4 優先消費

ミサイル発射時は、反転ミサイルを優先して消費する。

```text
if reverseMissileAmmo > 0:
    reverseMissileAmmo -= 1
    fireReverseMissile()
else:
    normalMissileAmmo -= 1
    fireNormalMissile()
```

### 8.5 反転ミサイル状態タイマー

ミサイル無限状態の条件には、反転ミサイル残弾そのものではなく、状態タイマーを使用する。

理由は、反転ミサイルが優先消費されるため、撃った瞬間に条件が崩れるとストレスになるためである。

```text
小型ミサイル吸収
  ↓
反転ミサイル残弾 + 1
反転ミサイル状態タイマー = max(現在値, 指定秒数)
```

推奨初期値:

```text
反転ミサイル状態タイマー: 8秒
```

### 8.6 反転ミサイル性能

初期値案:

| 項目 | ロックオンミサイル | 反転ミサイル |
|---|---:|---:|
| 威力 | 1.0 | 1.2 |
| 誘導性能 | 標準 | やや高い |
| 爆発範囲 | 標準 | やや広い |
| 色 | 白 / 青白 | シアン / 緑 / 紫系 |

---

## 9. 大型ミサイル吸収仕様

### 9.1 役割

大型ミサイルは、速度リソースである。

敵の大型ミサイルを吸収すると、自機の推進ブースターに変換され、**ソニックストリーム** が発動する。

### 9.2 吸収処理

大型ミサイルは価値が高いため、小型ミサイルより少し重い吸収条件にする。

推奨仕様:

```text
大型ミサイルがフィールドに接触
  ↓
短い捕獲ゲージを開始
  ↓
一定時間フィールド内に収める
  ↓
大型ミサイルを分解
  ↓
推進ブースターとして自機後方に接続
  ↓
ソニックストリーム開始
```

推奨初期値:

```text
捕獲時間: 0.25秒 ～ 0.40秒
```

プロトタイプ初期では、接触した瞬間に吸収成功としてもよい。

### 9.3 ソニックストリーム効果

推奨初期値:

| 項目 | 通常 | Sonic Stream |
|---|---:|---:|
| ロックオン範囲 | 1.0 | 1.0 |
| 最大ロック数 | 制限なし | 制限なし |
| 反転ミサイル威力 | 1.2 | 1.4 |
| 残骸吸収範囲 | 1.0 | 1.3 |
| スコア倍率 | 1.0 | 1.5 |
| レール進行速度 | 1.0 | 1.1 |
| 見た目速度 | 1.0 | 1.45 |

### 9.4 持続時間

推奨初期値:

```text
ソニックストリーム持続時間: 8秒
```

大型ミサイルを連続吸収した場合は、以下のどちらかにする。

```text
案A: 残り時間を最大値まで回復
案B: 残り時間を加算。ただし上限あり
```

プロトタイプでは案Aを推奨する。

---

## 10. 敵残骸吸収仕様

### 10.1 役割

敵残骸は、防御リソースである。

敵を撃破した後に発生する残骸、またはサルベージコアを吸収すると、自機のアーマーになる。

### 10.2 残骸生成

敵撃破時に、視認しやすいサルベージコアを発生させる。

理由は、物理破片を大量に拾わせると画面が見づらくなるためである。

```text
敵撃破
  ↓
爆発
  ↓
サルベージコア生成
```

### 10.3 吸収処理

```text
サルベージコアがフィールドに接触
  ↓
コアを消去
  ↓
アーマープレート + 1
  ↓
アーマー状態タイマー更新
  ↓
装甲装着エフェクト再生
```

### 10.4 アーマー性能

推奨仕様:

```text
アーマー1枚につき被弾1回を肩代わり
```

推奨初期値:

```text
最大アーマー枚数: 3
アーマー状態タイマー: 10秒
```

### 10.5 被弾時処理

```text
if armorPlateCount > 0:
    armorPlateCount -= 1
    preventDamage()
    playArmorBreakEffect()
else:
    applyDamage()
```

---

## 11. 3状態管理仕様

本作では、火力・速度・防御の3状態を独立して管理する。

| 状態 | 発生条件 | 判定 |
|---|---|---|
| 反転ミサイル状態 | 小型ミサイル吸収 | reverseMissileStateTimer > 0 |
| ソニックストリーム状態 | 大型ミサイル吸収 | sonicStreamTimer > 0 |
| アーマー状態 | 残骸吸収 | armorPlateCount > 0 または armorStateTimer > 0 |

### 11.1 状態更新

毎フレーム以下を行う。

```text
reverseMissileStateTimer -= deltaTime
sonicStreamTimer -= deltaTime
armorStateTimer -= deltaTime
missileInfinityTimer -= deltaTime
```

0未満になったら0に丸める。

### 11.2 READY判定

```text
if reverseMissileStateTimer > 0
and sonicStreamTimer > 0
and armorStateTimer > 0:
    missileInfinityReady = true
else:
    missileInfinityReady = false
```

アーマー状態の条件は、プロトタイプでは以下でもよい。

```text
armorPlateCount > 0
```

ただし、アーマーを被弾で失った瞬間にREADYが消えるため、最初は `armorStateTimer > 0` の方が扱いやすい。

---

## 12. ミサイル無限状態仕様

### 12.1 発動条件

```text
missileInfinityReady == true
```

### 12.2 発動方法

手動発動。

```text
READY中に特殊ボタンを押す
  ↓
ミサイル無限状態開始
```

### 12.3 発動時処理

```text
missileInfinityTimer = duration
missileInfinityReady = false
playMissileInfinityStartEffect()
updateHUDToMissileInfinity()
```

推奨初期値:

```text
持続時間: 6秒
```

### 12.4 効果

| 項目 | 通常 | Missile Infinity |
|---|---:|---:|
| マシンガン弾数 | 無限 | 無限 |
| ロックオン範囲 | 1.0 | 1.8 |
| 最大ロック数 | 制限なし | 制限なし |
| ミサイル発射間隔 | 標準 | 短縮 |
| 見た目速度 | 1.0 | 1.7 |
| スコア倍率 | 1.0 | 2.0 |

### 12.5 ミサイル発射

ミサイル無限状態中は、ロックオンミサイルを強化状態として発射する。
ロックオン範囲が拡張され、ロック済み対象すべてへ順番に発射する。

```text
if missileInfinityTimer > 0:
    expandLockOnRange()
    fireInfinityMissilesToAllLockedTargets()
else:
    fireLockOnMissilesToAllLockedTargets()
```

反転ミサイル残弾がある場合の扱いは2案ある。

```text
案A: 反転ミサイルも消費しない
案B: 反転ミサイルは通常通り優先消費する
```

プロトタイプでは、分かりやすさ重視で案Aを推奨する。

---

## 13. ロックオン仕様

### 13.1 基本

ロックオン可能対象が画面内、かつロックオン範囲内にいる場合、即時ロックする。
一度ロックした敵は、撃破されるか消滅するまでロック済みとして維持する。

```text
敵がロック範囲内に入る
  ↓
即時ロック済みになる
  ↓
撃破または消滅までロックを維持する
```

### 13.2 ロックオン範囲

ミサイル無限状態ではロックオン範囲を拡張する。

| 状態 | ロックオン範囲倍率 | 最大ロック数 |
|---|---:|---:|
| Cruise | 1.0 | 制限なし |
| Sonic Stream | 1.0 | 制限なし |
| Missile Infinity | 1.8 | 制限なし |

### 13.3 複数ロック

複数ロック済みの場合、ロックオンミサイル発射時に全ロック対象へ順番に発射する。

```text
ロック対象が複数ある
  ↓
ロック順にミサイルを発射する
```

---

## 14. ミサイル発射仕様

### 14.1 マシンガン

マシンガンは弾数無限で、ロックオン不要の前方攻撃とする。

```text
fireButton:
    fireMachineGun()
```

### 14.2 ロックオンミサイル

ロックオンミサイルは、ロック済み対象が存在する場合に発射できる。
1回の発射入力で、ロック済み対象すべてへ順番に発射する。

```text
missileButton:
    for target in lockedTargets:
        if reverseMissileAmmo > 0:
            fireReverseMissile(target)
            reverseMissileAmmo -= 1
        else:
            fireLockOnMissile(target)
```

### 14.3 ミサイル無限状態中

```text
if missileInfinityTimer > 0:
    for target in lockedTargets:
        fireInfinityMissile(target)
```

この場合、反転ミサイル残弾は消費しない。

### 14.4 ミサイルの色

| 種類 | 色 / 軌跡 |
|---|---|
| マシンガン | 黄白い短い弾 |
| ロックオンミサイル | 白煙、青白い軌跡 |
| 反転ミサイル | シアン、緑、紫などの特殊軌跡 |
| ミサイル無限中 | 反転ミサイル風の強化軌跡、大量発射感のある残光 |

---

## 15. 敵攻撃仕様

敵攻撃は、吸収可能かどうかを明確に分類する。

| 攻撃 | 吸収 | 対応 | 用途 |
|---|---|---|---|
| 小型ミサイル | 可能 | フィールドで吸収 | 反転ミサイル化 |
| 大型ミサイル | 可能 | フィールドで吸収 | ソニックストリーム化 |
| 通常弾 | 不可 | 移動で避ける | 基本プレッシャー |
| レーザー | 不可 | 位置取りで避ける | 範囲制御 |
| 爆風 | 不可 | 範囲外へ逃げる | 空間制限 |
| 赤い特殊弾 | 不可 | 避ける | フィールド対策 |

### 15.1 視認性ルール

吸収可能攻撃と吸収不可攻撃は、色と形を分ける。

```text
吸収可能:
  青、白、シアン、緑系
  ミサイル形状
  フィールドに近づくと反応する

吸収不可:
  赤、紫、黒系
  トゲ形状、レーザー、警告ライン
```

---

## 16. 速度仕様

### 16.1 速度状態

| 状態 | 説明 |
|---|---|
| Cruise | 通常状態 |
| Sonic Stream | 大型ミサイル吸収後の高速戦闘状態 |
| Missile Infinity | 3状態成立後の最大戦闘状態 |

### 16.2 速度の影響対象

速度状態は以下に影響する。

- ロックオン範囲
- 反転ミサイル性能
- 残骸吸収範囲
- スコア倍率
- FOV
- 速度線
- ブースト炎
- 音圧

### 16.3 実装方針

ステージ破綻を防ぐため、実レール速度は小さく変える。

一方、見た目速度と戦闘性能は大きく変える。

---

## 17. ダメージ仕様

### 17.1 被弾優先順位

被弾時は以下の順で処理する。

```text
1. アーマーがあるか確認
2. アーマーがあればアーマーを1枚消費してダメージ無効
3. アーマーがなければHPを減らす
4. HPが0ならゲームオーバー
```

### 17.2 被弾による状態解除

プロトタイプでは、被弾してもソニックストリームやミサイル無限状態は即解除しない方がよい。

理由は、爽快感が途切れやすいためである。

ただし、難易度を上げる場合は以下を検討する。

```text
アーマーなしで被弾
  → ソニックストリーム解除
```

---

## 18. UI仕様

### 18.1 表示情報

| UI | 内容 |
|---|---|
| HP | 自機体力 |
| ロック数 | 現在ロック中の敵数 |
| 反転ミサイル残弾 | 吸収ミサイル由来の特殊弾数 |
| フィールドゲージ | フィールド展開可能時間 |
| 反転ミサイル状態 | 火力状態タイマー |
| ソニックストリーム状態 | 速度状態タイマー |
| アーマー状態 | 防御状態タイマー / アーマー枚数 |
| Missile Infinity Ready | 3状態成立表示 |
| 速度状態 | Cruise / Sonic Stream / Missile Infinity |
| ロックオンUI | ロック済み対象 |

### 18.2 3状態スロット

画面左下などに、3状態をスロット表示する。

```text
FIRE   : 反転ミサイル状態
SPEED  : ソニックストリーム状態
ARMOR  : アーマー状態
```

3つが点灯したら、中央または上部に以下を表示する。

```text
MISSILE INFINITY READY
```

### 18.3 残弾UI

マシンガンとロックオンミサイルは弾数無限のため、残弾表示しない。
反転ミサイルのみ残弾を表示する。

```text
LOCK 06
REVERSE 04
```

または、コンパクトに以下でもよい。

```text
12 + 04R
```

---

## 19. エフェクト仕様

### 19.1 コンバートフィールド

- 自機周囲に円形リング
- 展開時に一瞬広がる波紋
- 吸収対象が触れると粒子化
- 吸収対象の色がフィールド色に変化

### 19.2 小型ミサイル吸収

- ミサイルが粒子化
- HUDの反転ミサイル残弾へ吸い込まれる演出
- 残弾数が加算される

### 19.3 大型ミサイル吸収

- 大型ミサイルが分解される
- 推進部だけが自機後方に接続される
- ブースト炎が強化される
- 速度線と衝撃波リングが発生する

### 19.4 残骸吸収

- サルベージコアが自機へ吸い込まれる
- 自機周囲に装甲プレートが装着される
- 被弾時にプレートが砕ける

### 19.5 ミサイル無限状態

- HUDが変形する
- ロックオンUIが増える
- ミサイル軌跡が派手になる
- FOVが広がる
- 速度線が最大化する

---

## 20. ステージ仕様

ルート分岐はしない。

1本のステージを段階的に構成し、各システムを順番に導入する。

### 20.1 ステージフェーズ

```text
Phase 1: 発進
  自機操作、カメラ、基本移動を見せる

Phase 2: 敵編隊戦
  マシンガン、マルチロックオン、ロックオンミサイルを使わせる

Phase 3: 小型ミサイル導入
  敵の小型ミサイルを吸収し、反転ミサイル化させる

Phase 4: 残骸吸収導入
  敵撃破後のサルベージコアを吸収し、アーマー化させる

Phase 5: 大型ミサイル導入
  大型ミサイルを吸収し、ソニックストリームを発動させる

Phase 6: 3状態合成
  FIRE / SPEED / ARMOR を同時成立させる

Phase 7: ミサイル無限状態
  拡張ロックオン範囲で敵編隊を殲滅させる

Phase 8: ボス戦
  全システムを使って巨大ボスを撃破させる
```

---

## 21. ボス仕様

### 21.1 ボスの目的

ボスは、コンバートフィールド、反転ミサイル、ソニックストリーム、アーマー、ミサイル無限状態をすべて使わせる総合戦とする。

### 21.2 ボスフェーズ

#### Phase 1: 通常戦

```text
ボスの砲台や随伴機と戦う
小型ミサイルを吸収して反転ミサイルを補充する
```

#### Phase 2: 大型ミサイル攻撃

```text
ボスが大型ミサイルを発射する
吸収するとソニックストリームが発動する
```

#### Phase 3: 装甲破壊

```text
ボス部位をロックオンして破壊する
部位破壊時にサルベージコアを発生させる
吸収してアーマー化する
```

#### Phase 4: ミサイル無限状態

```text
3状態を揃える
MISSILE INFINITY READY
手動発動
拡張ロックオン範囲でボスコアをロックオン
```

#### Phase 5: フィニッシュ

```text
コア破壊
大型爆発
高速離脱
ステージクリア
```

---

## 22. 敵タイプ仕様

### 22.1 小型敵

| 項目 | 内容 |
|---|---|
| 役割 | 通常ロックオン対象 |
| 攻撃 | 小型ミサイル、通常弾 |
| 撃破後 | サルベージコアを出す場合あり |

### 22.2 ミサイル敵

| 項目 | 内容 |
|---|---|
| 役割 | 小型ミサイル供給役 |
| 攻撃 | 小型ミサイルを多めに発射 |
| 狙い | 反転ミサイル補充の練習 |

### 22.3 大型敵

| 項目 | 内容 |
|---|---|
| 役割 | 大型ミサイル供給役 |
| 攻撃 | 大型ミサイル、レーザー |
| 狙い | ソニックストリーム発動の起点 |

### 22.4 ボス部位

| 項目 | 内容 |
|---|---|
| 役割 | 部位破壊対象 |
| 攻撃 | 小型ミサイル、大型ミサイル、レーザー |
| 撃破後 | サルベージコアを発生 |

---

## 23. データ設計案

### 23.1 変換対象種別

```cpp
enum class ConvertTargetType
{
    SmallMissile,
    LargeMissile,
    SalvageCore,
};
```

### 23.2 プレイヤー状態

```cpp
struct PlayerCombatState
{
    int normalMissileAmmo;
    int reverseMissileAmmo;

    float fieldGauge;
    float reverseMissileStateTimer;
    float sonicStreamTimer;
    float armorStateTimer;
    float missileInfinityTimer;

    int armorPlateCount;
    bool missileInfinityReady;
};
```

### 23.3 速度状態

```cpp
enum class SpeedState
{
    Cruise,
    SonicStream,
    MissileInfinity,
};
```

---

## 24. 更新処理フロー

1フレームの大まかな処理は以下。

```text
1. 入力更新
2. レール位置更新
3. 自機オフセット移動更新
4. 敵更新
5. 敵弾更新
6. コンバートフィールド更新
7. 吸収判定
8. ロックオン更新
9. ミサイル発射処理
10. 衝突判定
11. 状態タイマー更新
12. 速度状態更新
13. UI更新
14. 描画
```

---

## 25. プロトタイプ用初期パラメータ

| 項目 | 値 |
|---|---:|
| マシンガン弾数 | 無限 |
| 反転ミサイル最大弾数 | 12 |
| フィールド最大ゲージ | 1.5秒分 |
| フィールド回復時間 | 2.0秒 |
| 反転ミサイル状態時間 | 8.0秒 |
| ソニックストリーム時間 | 8.0秒 |
| アーマー状態時間 | 10.0秒 |
| 最大アーマー枚数 | 3 |
| ミサイル無限時間 | 6.0秒 |
| 通常時ロックオン範囲倍率 | 1.0 |
| ミサイル無限時ロックオン範囲倍率 | 1.8 |
| 最大ロック数 | 制限なし |

---

## 26. 実装優先順位

### Step 1: 半レール基礎

```text
レール移動
カメラ追従
自機オフセット移動
敵配置
```

### Step 2: 基本攻撃

```text
マシンガン
マルチロックオン
ロックオンミサイル
敵撃破
爆発
```

### Step 3: コンバートフィールド

```text
フィールド展開
吸収判定
吸収可能対象の識別
```

### Step 4: 小型ミサイル変換

```text
小型ミサイル吸収
反転ミサイル残弾化
優先消費
色違い弾道
```

### Step 5: 大型ミサイル変換

```text
大型ミサイル吸収
ブースター接続
ソニックストリーム
速度演出
発射テンポ上昇
```

### Step 6: 残骸変換

```text
敵撃破時のサルベージコア生成
残骸吸収
アーマー生成
被弾肩代わり
```

### Step 7: ミサイル無限状態

```text
3状態判定
READY表示
手動発動
一定時間ミサイル無限
```

### Step 8: ボス戦

```text
巨大ボス
部位ロックオン
大型ミサイル攻撃
部位破壊
ミサイル無限でコア破壊
```

---

## 27. 調整用ImGui項目

開発中は、以下をImGuiで調整できるようにするとよい。

```text
フィールド半径
フィールドゲージ最大値
フィールド回復速度
反転ミサイル威力
反転ミサイル最大弾数
反転ミサイル状態時間
ソニックストリーム時間
ソニックストリーム時のFOV倍率
ソニックストリーム時のロックオン倍率
アーマー最大枚数
アーマー状態時間
ミサイル無限時間
ミサイル無限時のロックオン範囲倍率
ミサイル無限時の発射間隔
```

---

## 28. 仕様上の注意点

### 28.1 小型ミサイルを自機周囲に保持しない

小型ミサイルを自機周囲に大量表示すると画面が見づらくなる。

そのため、小型ミサイルは吸収後に残弾UIへ変換し、発射時だけ派手に見せる。

### 28.2 大型ミサイルは見た目に出す

大型ミサイルは速度リソースなので、自機後方にブースターとして接続される見た目を出す。

これは本作の見せ場になる。

### 28.3 残骸はサルベージコア化する

物理破片を全部拾わせると制御しづらい。

ゲーム上はサルベージコアを吸収対象にする。

### 28.4 ミサイル無限状態は出しやすくする

3状態を揃えるのが難しすぎると、ゲームの見せ場が出ない。

プロトタイプでは、各状態タイマーを長めにし、意図的に揃えやすくする。

### 28.5 吸収不可攻撃を必ず用意する

フィールドが万能になるとゲームが単調になる。

通常弾、レーザー、赤い特殊弾など、フィールドで吸えない攻撃を必ず配置する。

---

## 29. 最小完成ライン

プロトタイプとして最低限完成とみなすラインは以下。

```text
半レールで飛べる
敵をロックオンできる
マシンガンとロックオンミサイルで敵を倒せる
小型ミサイルを吸収して反転ミサイルにできる
大型ミサイルを吸収してソニックストリームになれる
敵残骸を吸収してアーマーにできる
3状態を揃えてミサイル無限状態を発動できる
ミサイル無限状態で敵編隊を殲滅できる
```

ボス戦は次の完成ラインとして扱う。

---

## 30. 完成時に見せたいプレイ映像

理想的なプレイ映像は以下。

```text
1. 高速で海上または峡谷を飛ぶ
2. マシンガンで削りつつ、敵編隊をマルチロックオンしてロックオンミサイルで撃破
3. 敵小型ミサイルをコンバートフィールドで吸収
4. 反転ミサイルを色違いで発射
5. 敵残骸を吸収してアーマーを装着
6. 大型ミサイルを吸収してソニックストリーム発動
7. 速度演出が大きく変化
8. 3状態が揃って MISSILE INFINITY READY
9. ミサイル無限状態を発動
10. 拡張ロックオン範囲と連続ロックオンミサイルで敵を殲滅
11. 巨大ボスのコアを破壊
12. 高速離脱してクリア
```

---

## 31. まとめ

本作の仕様は、以下の3変換を中心に構成する。

```text
小型ミサイル → 反転ミサイル残弾 → 火力
大型ミサイル → ソニックストリーム → 速度
敵残骸 → アーマー → 防御
```

これらを同時に成立させることで、

```text
ミサイル無限状態
```

に移行する。

プレイヤーは敵の攻撃と撃破結果を利用しながら、火力・速度・防御を揃え、高速戦闘状態で敵を殲滅する。


